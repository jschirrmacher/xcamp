<html lang="de">
<head>
    <meta charset="UTF-8">
    <title>XCamp 2018 Zugang</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <base href="{{baseUrl}}">
    <link rel="stylesheet" href="ticket.css">
</head>
<body>
{{> menu}}
{{> logo}}
<div id="main">
    <div class="container">
        <h1>Passwort erforderlich</h1>

        <p>Diese Seite ist mit einem Passwort geschützt.</p>

        <form id="login-form" action="login" method="post">
            <label id="email">E-Mail-Adresse
                <input type="email" name="email">
            </label>
            <label>Passwort
                <input type="password" name="password">
            </label>
            <input type="hidden" name="username" value="{{accessCode}}">
            <input type="hidden" name="url" value="{{url}}">
            <button type="submit">Login</button>
        </form>

        <h2>Passwort vergessen?</h2>
        <a class="button wide" href="accounts/{{accessCode}}/password">Neues Passwort setzen</a>
    </div>
</div>
{{> footer}}
<script src="common.js"></script>
<script src="login.js"></script>
</body>
</html>
